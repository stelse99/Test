## CPU Hyper-v 적용 되어 있어야 한다.
  요즘은 기본적으로 되어 있다.

## WSL2(Windows Subsystem for Linux 2.0) 을
   install 및 upgrade.
   https://docs.docker.com/

## 설치후 확인 방법.
docker images
docker run -d -p 80:80 --name web nginx
docker ps

## container 중지
docker rm -f web
## image 삭제
docker rmi nginx
